# Dockerfile.backend
FROM python:3.11-slim

WORKDIR /app
COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY backend/ .

# ถ้าใช้ FastAPI + uvicorn
CMD ["uvicorn", "earnings_api:app", "--host", "0.0.0.0", "--port", "3001"]
